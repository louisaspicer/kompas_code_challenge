extends ../layout


block content
    h1.
         #{title}
    ul
        each user, i in users
            li
                = user.firstName
                = user.lastName
                = user.age
                = user.city
